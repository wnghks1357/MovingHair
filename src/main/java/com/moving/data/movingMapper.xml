<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movingHair">
	<!-- 
	<resultMap type="userRvo" id="userRvoMap">
		<result column="USER_ID" property="userId" />
	</resultMap>
	 -->
	
	<select id="idJungbokCheck" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM mh_user WHERE USER_ID = #{userId}
	</select>
	
	<select id="phoneJungbokCheck" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM mh_user WHERE USER_PHONE = #{userPhone}
	</select>
	
	<!-- TODO -->
	<insert id="joinProc" parameterType="userPvo">
		INSERT INTO mh_user(
								USER_ID
								, USER_PWD
								, USER_TYPE
								, USER_NAME
								, USER_PHONE
								, USER_EMAIL
								, USER_STATUS
								, USER_ZIP
								, USER_ADDR1
								, USER_ADDR2
								, USER_BIRTHDAY
								, USER_SEX
								, RECEIVE_EMAIL
								, RECEIVE_SMS
								, CREATED_TYPE
								, REQUIRED_AGREE
								, SELECT_AGREE
								, CREATED__DATE
								, UPDATE_DATE
							) VALUES (
									 #{userId}
									, #{userPwd}
									, #{userType}
									, #{userName}
									, #{userPhone}
									, #{userEmail}
									, 'A'
									, #{userZip}
									, #{userAddr1}
									, #{userAddr2}
									, #{userBirthday}
									, 'M'
									, 'Y'
									, 'Y'
									, 'M'
									, 'Y'
									, 'Y'
									, NOW()
									, NOW()
								)
								
	</insert>

	<select id="loginProc" parameterType="userPvo" resultType="userRvo">
		SELECT USER_ID AS userId, USER_TYPE AS userType FROM mh_user WHERE USER_ID = #{userId} AND USER_PWD = #{userPwd}
	</select>
 
</mapper>